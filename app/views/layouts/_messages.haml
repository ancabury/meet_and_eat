- flash.each do |name, msg|
  - if msg.is_a?(String)
    %div{class: "alert alert-#{flash_type(name)} container"}
      %button.close{"aria-hidden" => "true", "data-dismiss" => "alert", type: "button"} &times;
      = content_tag :div, msg, id: "flash_#{name}"
